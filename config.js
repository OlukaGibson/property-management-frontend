const config = {
    // baseURL: "http://127.0.0.1:5000"
    // baseURL: "https://delicate-factually-mastiff.ngrok-free.app"
    baseURL: '/api' // Using relative path to work with Vercel rewrites
    // Alternative direct approach (not recommended with Vercel setup):
    // baseURL: 'http://35.202.217.87'
};

export default config;